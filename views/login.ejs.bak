<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <style>
    /* Estilo para a página de login */
    body {
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #4CAF50, #2196F3); /* Cor de fundo com gradiente */
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      position: relative;
      flex-direction: column;
    }

    /* Animação de brilho no nome */
    .title {
      font-size: 36px;
      font-weight: bold;
      margin-bottom: 20px;
      opacity: 0;
      animation: fadeIn 2s ease-out forwards, glow 3s infinite alternate 2s;
    }

    .title span {
      display: inline-block;
    }

    .title .black {
      color: black;
    }

    .title .blue {
      color: #007BFF; /* Azul mais forte */
    }

    /* Estilo para o container de login */
    .login-container {
      background-color: rgba(255, 255, 255, 0.9);
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      width: 280px; /* Largura do card ajustada */
      z-index: 1; /* Garante que o card de login fique acima do rodapé */
    }

    .login-container h1 {
      text-align: center;
      margin-bottom: 20px;
    }

    .login-container label {
      font-weight: bold;
    }

    .login-container input {
      width: 100%;
      padding: 10px;
      margin: 10px 0;
      border: 1px solid #ccc;
      border-radius: 4px;
      box-sizing: border-box; /* Garante que o padding não ultrapasse a largura do card */
    }

    .login-container button {
      width: 100%;
      padding: 10px;
      background-color: #4CAF50;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    .login-container button:hover {
      background-color: #45a049;
    }

    /* Estilo da marca d'água */
    .watermark {
      position: absolute;
      bottom: 20px; /* Ajuste para garantir que não fique colado ao card */
      left: 50%;
      transform: translateX(-50%);
      font-size: 14px;
      color: rgba(255, 255, 255, 0.7);
      text-align: center;
      z-index: 0; /* Fica atrás do conteúdo */
    }

    /* Estilo para a mensagem sobre lealdade */
    .loyalty-message {
      font-size: 18px;
      font-weight: bold;
      color: white;
      text-align: center;
      margin-top: 20px;
    }

    /* Animações */
    @keyframes fadeIn {
      0% {
        opacity: 0;
        transform: translateY(-20px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes glow {
      0% {
        text-shadow: 0 0 5px white, 0 0 10px lightblue, 0 0 15px lightblue;
      }
      100% {
        text-shadow: 0 0 15px white, 0 0 30px lightblue, 0 0 45px lightblue;
      }
    }

  </style>
</head>
<body>
  <!-- Título com animação de brilho -->
  <div class="title">
    <span class="black">L</span>
    <span class="blue">O</span>
    <span class="black">Y</span>
    <span class="blue">A</span>
    <span class="black">L</span>
    <span class="blue">T</span>
    <span class="black">Y</span>
  </div>

  <div class="login-container">
    <h1>Bem-vindo! Faça login</h1>
    <form id="login-form" method="POST" action="/login">
      <label for="username">Nome de usuário</label>
      <input type="text" id="username" name="username" required>

      <label for="password">Senha</label>
      <input type="password" id="password" name="password" required>

      <button type="submit">Entrar</button>
    </form>
  </div>

  <!-- Mensagem sobre lealdade -->
  <div class="loyalty-message">
    A lealdade é a base de tudo. Mantenha-se fiel aos seus valores e aos que você acredita.
  </div>

  <!-- Marca d'água -->
  <div class="watermark">feito por @nielzinxyz - todos os direitos reservados a LTY</div>

  <script>
    // Função para validar o formulário antes de enviar
    document.getElementById('login-form').addEventListener('submit', function(event) {
      const username = document.getElementById('username').value;
      const password = document.getElementById('password').value;

      if (!username || !password) {
        alert('Por favor, preencha todos os campos.');
        event.preventDefault(); // Impede o envio do formulário se os campos estiverem vazios
      }
    });
  </script>
</body>
</html>